export interface IFooterInfo {
  title: string;
  firstColumn: string[];
  secondColumn: string[];
}

export interface IAuditsContent {
  title: string;
  text: string;
  links: string[];
}

export interface ITypeItem {
  title: string;
  text: string;
}